spring:
  datasource:
    url: jdbc:h2:mem:registrationdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

services:
  course-service:
    base-url: http://localhost:8082
